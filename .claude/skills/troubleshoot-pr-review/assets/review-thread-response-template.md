# 리뷰 스레드 답글 템플릿

판단 결과에 맞는 템플릿 하나만 사용한다. 중괄호 자리표시자를 실제 값으로 바꾸고, 해당 없는 줄은 삭제한다.

## 수정 필요

반영했습니다.

- 원인: {문제를 만든 근본 원인}
- 변경: {변경한 동작과 PR에서 접근 가능한 파일 또는 커밋 링크}
- 검증: `{핵심 검증 명령}` — {통과 결과와 확인한 동작}
- 기록: [{트러블슈팅 문서 제목}]({PR에서 접근 가능한 문서 링크})

## 이미 해결

현재 PR에서 요청 사항이 충족된 것을 확인했습니다.

- 근거: {현재 구현이 요청을 충족하는 코드·계약 근거와 링크}
- 검증: `{검증 명령 또는 확인 절차}` — {관찰 결과}
- 기록: [{트러블슈팅 문서 제목}]({PR에서 접근 가능한 문서 링크})

## 수정 불필요

확인 결과 이번 PR에서는 변경하지 않았습니다.

- 판단 근거: {리뷰 요청을 적용하지 않는 구체적인 이유}
- 계약/코드: {판단을 뒷받침하는 계약 또는 코드 링크}
- 검증: `{검증 명령 또는 확인 절차}` — {관찰 결과}
- 기록: [{트러블슈팅 문서 제목}]({PR에서 접근 가능한 문서 링크})

## 결정 필요

현재는 결정이 필요해 이 스레드를 해결 처리하지 않았습니다.

- 필요한 결정: {사용자 또는 소유자가 선택할 사항}
- 영향 범위: {각 선택이 API, 데이터, 동작 등에 미치는 영향}
- 확인한 근거: {계약·코드·재현 결과 링크}
- 다음 주체: {결정할 사람 또는 팀}
- 기록: [{트러블슈팅 문서 제목}]({PR에서 접근 가능한 문서 링크})
