package com.masiton.restaurant.application.naturallanguage;

/** 자연어 parser가 기존 목록 조회 조건으로 변환할 수 있는 필드다. */
public enum ConditionField {
    QUERY,
    DISTRICT,
    CATEGORY,
    CREATOR_ID,
    TAGS
}
