# WebSocket·HTTP Upgrade 중계용 매핑 — M2-08.
# Next.js 개발 서버가 아닌 운영 빌드에서도 Upgrade 헤더가 오는 경로가 있어
# hop-by-hop 헤더를 그대로 넘기지 않고 요청에 따라 결정한다.
# masiton.click.conf의 `$connection_upgrade`가 이 매핑을 참조한다.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
